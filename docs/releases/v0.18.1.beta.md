## Added
- Added validation of legacy item and Space preference data before Room migration is declared complete.

## Changed
- Legacy source JSON is kept when Dayline cannot decode it completely, so a partial/corrupt migration cannot silently discard the original data.

## Fixed
- Beta identity aligned to 0.18.1.beta / versionCode 1801.
