## Added
- Added a one-time post-update What's new sheet after app upgrades.
- Added persisted previous-version and pending history tracking so skipped betas are reconstructed exactly.

## Changed
- Post-update notes reuse the same exact per-release changelog cards as update checks and Settings → Changelog.
- Failed release-history fetches retain the original upgrade range and retry on the next launch.

## Fixed
- Fixed the newly installed APK losing the previous version needed to explain what changed.
- Beta identity aligned to 0.18.9.beta / versionCode 1809.
