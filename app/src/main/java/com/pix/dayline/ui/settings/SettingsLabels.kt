package com.pix.dayline.ui.settings

/**
 * Stable semantic labels kept separate from the compact visual copy.
 *
 * The Settings UI can use shorter visible labels while release validation and
 * accessibility-oriented tooling still have a durable feature name to anchor to.
 */
internal object SettingsLabels {
    const val CALENDAR_SYNC_HEALTH = "Sync health"
}
